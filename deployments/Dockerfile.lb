FROM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /out/nimbuslb ./cmd/nimbuslb

FROM alpine:3.20
COPY --from=build /out/nimbuslb /usr/local/bin/nimbuslb
EXPOSE 8080 9090
ENTRYPOINT ["nimbuslb"]
